# 舟游记 1.0 交接说明

更新时间：2026-06-17

## 项目位置

- 源码项目：`/Users/sunkai/Codex/舟游记1.0`
- 微信开发工具当前预览目录：`/Users/sunkai/Codex/舟游记1.0/unpackage/dist/dev/mp-weixin`

## 当前开发环境

- 小程序 AppID：`wx74a85cc2e9dcc6e4`
- 云开发环境 ID：`cloud1-d8gsb809j07104a62`
- 微信开发工具 CLI：`/Applications/wechatwebdevtools.app/Contents/MacOS/cli`
- 用户当前主要通过微信开发工具预览 `unpackage/dist/dev/mp-weixin`

## 已完成事项

### 1. 后端头像和用户名同步

- 用户登录后会调用云函数 `api`。
- 已让用户中心使用云端用户数据，不再只依赖模拟用户名。
- 用户修改昵称/头像后会写入后端并同步本地缓存。
- 退出登录再登录后，会优先从后端拉取用户资料，避免重复要求输入用户名。

相关文件：

- `cloudfunctions/api/index.js`
- `utils/cloud-object.js`
- `pages/user/user.uvue`
- `pages/user/profile.uvue`
- `unpackage/dist/dev/mp-weixin/common/cloud-api.js`
- `unpackage/dist/dev/mp-weixin/pages/user/user.js`

### 2. 登录页 logo 替换

- 已从用户提供的 AI 文件中提取舟游记 VI 标识。
- 新 logo 文件已放入：
  - `static/zy/logo-mark.png`
  - `unpackage/dist/dev/mp-weixin/static/zy/logo-mark.png`
- 登录页和“我的”页未登录状态都已改用新 logo。

相关文件：

- `pages/user/login.uvue`
- `pages/user/user.uvue`
- `unpackage/dist/dev/mp-weixin/common/assets.js`
- `unpackage/dist/dev/mp-weixin/pages/user/login.*`
- `unpackage/dist/dev/mp-weixin/pages/user/user.*`

### 3. 登录协议勾选打通

- 协议勾选不再是静态展示。
- 默认未勾选。
- 未勾选时点击“微信一键登录”，会提示：`请先勾选并同意协议`。
- 勾选后方可继续登录。
- 《用户服务协议》和《隐私政策》都能点击查看弹窗。

已同步到：

- 独立登录页：`pages/user/login.uvue`
- “我的”页未登录状态：`pages/user/user.uvue`
- 微信预览生成目录：`unpackage/dist/dev/mp-weixin/pages/user/`

### 4. 登录页 UI 调整

- 收紧了顶部 logo 区域。
- 缩短登录卡片高度。
- 压缩按钮、协议、底部装饰之间的间距。
- 弱化并缩小底部海岛装饰。
- 独立登录页增加 `disableScroll: true`，避免出现下拉预留空白。
- “我的”页未登录状态也同步做了一屏内布局收紧。

相关文件：

- `pages/user/login.uvue`
- `pages/user/user.uvue`
- `pages.json`
- `unpackage/dist/dev/mp-weixin/pages/user/login.wxss`
- `unpackage/dist/dev/mp-weixin/pages/user/login.json`
- `unpackage/dist/dev/mp-weixin/pages/user/user.wxss`

## 已验证

已执行并通过：

```bash
node --check unpackage/dist/dev/mp-weixin/pages/user/login.js
node --check unpackage/dist/dev/mp-weixin/pages/user/user.js
```

已执行 JSON 检查并通过：

```bash
node -e "JSON.parse(require('fs').readFileSync('pages.json','utf8')); JSON.parse(require('fs').readFileSync('unpackage/dist/dev/mp-weixin/pages/user/login.json','utf8')); console.log('json ok')"
```

微信开发工具 CLI 预览成功：

```bash
/Applications/wechatwebdevtools.app/Contents/MacOS/cli preview --project /Users/sunkai/Codex/舟游记1.0/unpackage/dist/dev/mp-weixin --qr-format terminal
```

预览结果：

- 使用 AppID：`wx74a85cc2e9dcc6e4`
- 预览包大小约：`1.3 MB`
- CLI 返回：`preview ✔`

## 下一步建议

1. 用微信扫码预览。
2. 进入“我的”页未登录状态，检查 logo、卡片高度和底部是否还有下拉空白。
3. 不勾选协议直接登录，确认出现提示。
4. 勾选协议后登录，确认后端头像昵称仍能同步。
5. 点击《用户服务协议》《隐私政策》，确认弹窗正常。

## 注意事项

- 源码目录和微信预览目录都做了同步修改。后续如果用 HBuilderX 重新编译，`unpackage/dist/dev/mp-weixin` 可能被覆盖，应以 `pages/**/*.uvue`、`pages.json`、`static/zy/logo-mark.png` 等源文件为准。
- 不建议直接长期维护 `unpackage` 下的文件；这次同步是因为当前微信开发工具正在预览该目录。
